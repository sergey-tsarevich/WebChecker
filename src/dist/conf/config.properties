# If drop sqlite db before start(after that db will be recreated)
### Be carefull -> it is possible to drop all data!
dropDb=false
# every day at 4 and 17 o'clock
#viewerCronTrigger=0 0 4,17 * * ?
# run every minute
viewerCronTrigger=0 */1 * * * ?
# Path SqLite database todo: fix to use \ too, and place into *.sh script folder
#dbPath=h:/Projects/lab/WebChecker/src/dist/data/inter_store
#dbPath=/home/tss/Apps/Work/WebChecker/src/dist/data/inter_store

## SMTP server setting for email notifier ##
 # comma separated sequence of addresses
emailTo = me@changeMe.com
smtpHost = smtp.gmail.com
smtpPort = 465
smtpAuth = true
smtpLogin = changeMe
smtpPassword = changeMe
